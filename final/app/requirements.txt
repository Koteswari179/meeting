# Core Framework
Django==5.2

# Database
mysqlclient==2.2.7          # Use if connecting Django with MySQL
# or use the alternative if mysqlclient fails to install:
# PyMySQL==1.1.0

# Authentication and Password Hashing
bcrypt==4.1.3

# Email Support
django-environ==0.11.2      # For environment variable management
requests==2.32.3            # For API/email integrations if needed

# PDF / File Generation (if reports or downloads are used)
reportlab==4.2.0

# Date and Time
pytz==2024.1

# Django utilities
django-crispy-forms==2.2
crispy-bootstrap5==0.7

# For handling CORS or APIs (if frontend connects separately)


# For JSON / REST API (optional, if you use API endpoints)


# For handling OTP timestamps / token expiry
python-dateutil==2.9.0

# Optional tools for admin dashboard
django-filter==24.3
